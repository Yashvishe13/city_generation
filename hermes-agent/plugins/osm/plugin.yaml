name: osm
version: "0.2.0"
description: >
  The contract an OSM -> Unreal Engine city pipeline is written against, as seven
  loadable skills, plus one tool that verifies a generated scene against the original
  OpenStreetMap extract. Fetching and analysis are written by the agent into its own
  pipeline, not provided as tools.
author: city_generation project
