Generate the OpenStreetMap -> Unreal pipeline for the area nyc_midtown
(Midtown Manhattan, bbox south=40.7500 west=-73.9860 north=40.7555 east=-73.9790).

Load the skill osm:pipeline-plan first. It tells you how to proceed, which other skills to
load, what to measure before deciding anything, and what this project has already got
wrong that you should design against.

WRITE THESE FILES. They will be checked on disk; a description of work that does not
exist as files counts as a failed run:
  agent_scripts/nyc_midtown/PLAN.md
  agent_scripts/nyc_midtown/pipeline.py
  data/ue/nyc_midtown/scene.json   (produced by RUNNING pipeline.py, not written by hand)

pipeline.py: standard library only, self-contained from Overpass through to scene.json,
deterministic, with a --verify mode.

Then call osm_verify_scene for real and quote what it returns. If a tool call fails, say
so rather than describing what it would have said.

Report what you measured, what you decided and why, and the verifier's actual output.
